Fix 1 — the progress ring. The circular SVG ring next to the score is showing as just an outline with no fill progress. It needs to actually show 74% filled in brick red #A63D2F using a stroke-dasharray animation. The ring should animate from 0 to 74% on page load over 800ms with an ease-out curve. The "/100" text should sit centered inside the ring, not beside it. The "74" large number and the ring should be on the same baseline, side by side with 24px gap.
Fix 2 — the map pin. Replace the default blue Leaflet marker with a custom brick red circle marker — a simple filled circle 12px diameter, #A63D2F fill, white 2px border, with a subtle pulse animation radiating outward in brick red to draw the eye. No default teardrop pin shape.
Fix 3 — median income showing a dash. When median income data is unavailable show "Not reported" in 13px italic gray instead of a bare dash. Applies to any empty field in the census tract dossier grid.
Fix 4 — the "What we found" and "Census tract" headings are misaligned. They sit on the same row but different visual weights. Make both exactly 32px Playfair Display bold so they feel like a unified two-column layout. Add a 1px horizontal rule below each heading that stretches the full width of its column.
Fix 5 — the comparison bar chart labels. "This address," "City average," "State average," "National average" are left-aligned but the bars start at different indents making it look ragged. Set a fixed left column width of 140px for all labels, then bars start uniformly at 140px. Numbers on the right should be bold for "This address" and regular weight for the others.
Fix 6 — social share messages. The Twitter/X, LinkedIn, and Facebook share links should open with a pre-written message: "I checked my home's lead pipe risk using Plumbum, a free open-source public health tool. My address scored 74/100 — High Risk. Check yours:" followed by the URL. Use encodeURIComponent on the message. This is how it spreads.
Fix 7 — the footer. The mini footer strip at the top of the dark footer reads "Lead pipe risk assessment · Data from EPA ECHO, Census Bureau, and USGS" — add "· Not affiliated with any government agency" at the end. The logo in the footer should be the PNG from /public/logo.png at 20px height, not the text "Plumbum."