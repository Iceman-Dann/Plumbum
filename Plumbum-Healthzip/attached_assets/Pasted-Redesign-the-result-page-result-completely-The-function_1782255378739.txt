Redesign the result page /result completely. The functionality is correct — keep all the API logic, the map, the risk factors, the census tract data, and the sharing strip. Only rebuild the visual design.
The risk score is the hero of this page and right now it's tiny. Make "74" at least 140px Playfair Display bold in brick red #A63D2F. It should be the first thing that hits you when the page loads — like a verdict. "High Risk" below it in 28px Playfair italic gray. To the right of the score inline, add a circular progress ring SVG showing 74/100 filled in brick red. This one visual element transforms the page from a result into a statement.
Add a full-width colored alert banner directly below the nav for high risk scores. Brick red background #A63D2F, white text 14px Inter centered: "⚠ This address shows a HIGH RISK score. We strongly recommend getting your water tested. Get a free test kit →" Only shows when score is above 60. Below 40 shows green. Middle shows amber.
The map needs to be a dark basemap not the colorful OpenStreetMap default. Switch the Leaflet tile layer to CartoDB Dark Matter or CartoDB Positron — they look professional and let the brick red risk coloring of census tracts stand out dramatically instead of competing with road colors. The map should be 480px tall not the current height.
Risk factors section is too generic. "Detected Factor" as the title on every single factor is lazy — replace it with the actual specific factor name as the title: "Pre-1986 Construction" / "EPA Violation on Record" / "Proximity to Superfund Site." Each factor gets a severity indicator — a small pill on the right side of each row saying "HIGH" "MODERATE" or "LOW" in the corresponding color. Below each factor title add a 13px gray explanation sentence that contextualizes why this matters for lead risk specifically.
Your Census Tract section is a gray box with barely any data. Expand it to a proper data grid — 2x3 layout of six data points each with an 11px all-caps gray label and a bold value below: FIPS Code / Median Build Year / Water District / EPA Violations (10yr) / Median Income / % Pre-1986 Housing. Make it feel like a dossier on this specific location.
"What Should You Do?" section needs a dark background — #0F0F0D — to visually separate it and give it the weight of a call to action. White card borders, white text headings, gray body text. The three cards should have a 3px top border in brick red. The action links at the bottom of each card should be brick red underlined, not blue default browser links.
Add a completely new section above the sharing strip called "How does your neighborhood compare?" Show a horizontal bar chart — plain CSS bars no library needed — comparing this address's score to the city average, the state average, and the national average. Four bars, labeled, the searched address bar in brick red, others in gray. This is the detail that makes someone screenshot and share the result.
The sharing strip is good but add one line below the copy link: "Or share directly:" with plain text links — "Twitter/X · LinkedIn · Facebook" — that pre-populate a share message: "I just checked my home's lead pipe risk on Plumbum. My score: 74/100. Check yours: [url]" That social sharing mechanic is how this tool goes viral.
Fix the dead space at the bottom of the page — there's a large empty area below the sharing strip. Either tighten the padding or add a footer consistent with the rest of the site.
Global fix for this page: Every heading currently uses sentence case inconsistently — "What Should You Do?" mixes cases. Standardize: all section headers are 11px all-caps gray letter-spacing 0.12em as labels, with the actual headline in Playfair below it. No title case anywhere.
